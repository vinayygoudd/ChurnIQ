# ML Pipeline

The training pipeline uses leakage-safe preprocessing, stratified five-fold CV, candidate classification models, controlled tuning, model selection by documented CV F1, and joblib serialization.
